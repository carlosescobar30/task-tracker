import java.io.File;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.*;

public class Main {
    public static void main(String[] args) {
        /*File file = new File("tasks.json");
        String fileString = ReadTasks.readTasks(file);
        Map <Integer, Task> taskMap = new HashMap<>();
        List<Task> tasks = new ArrayList<>(Formatters.formatToTasks(fileString));
        for (Task task : tasks){
            taskMap.put(task.getId(),task);
        }
        System.out.println("Por favor, ingrese el id que quiero modificar");
        Scanner scanner = new Scanner(System.in);
        int id = scanner.nextInt();
        scanner.nextLine();
        System.out.println("Por favor, ingrese la nueva descripcion");
        String description = scanner.nextLine();
        Task task =  taskMap.get(id);
        task.setDescription(description);*/

    }
}